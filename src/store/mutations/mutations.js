export default {
  SET_GRAPHS_TO_STATE: (state, graph) => {
    state.graph = graph;
  },
  SET_DIRECTIONS_TO_STATE: (state, directions) => {
    state.directions = directions;
  },
  SET_FLIGHT_NUMBERS_TO_STATE: (state, flightNumbers) => {
    state.flightNumbers = flightNumbers;
  },
  SET_BOOKING_CLASSES_TO_STATE: (state, bookingClasses) => {
    state.bookingClasses = bookingClasses;
  },
  SET_GRAPH_TO_STATE: (state, graph) => {
    state.graph = graph;
  },
};
